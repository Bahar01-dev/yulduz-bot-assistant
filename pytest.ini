[pytest]
# Набор тестов написан под авто-режим pytest-asyncio: async-тесты и async-фикстуры
# (напр. clean_db в tests/conftest.py) не требуют ручного @pytest_asyncio.fixture.
# Без этого pytest-asyncio ≥1.x (strict по умолчанию) роняет setup async-фикстур.
asyncio_mode = auto
testpaths = tests
